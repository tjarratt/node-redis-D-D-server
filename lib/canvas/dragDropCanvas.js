(function( $ ){
  $.fn.myPlugin = function() {
  
    //init, take options (grid size, show grid, annotations?)
    
    //bindings for textArea onMouseOver, mousedown, mousemove, mouseup

  };
})( jQuery );